- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: Update repositories
      dnf:
        update_cache: yes
      changed_when: False

  tasks:
    - include: tasks/packages-fedora.yml
